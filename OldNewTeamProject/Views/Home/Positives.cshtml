@{
    ViewBag.Title = "Positives";
}

<div class="jumbotron">
    <h1>Techs comparator</h1>
    <div description>edi-kvo si</div>
    <p class="lead"></p>
</div>

<div class="actionLinks">
    <div class="btn-link">@Html.ActionLink("Home", "Index", "Home")</div>
    <div class="btn-link">@Html.ActionLink("Ratios", "Ratios", "Home")</div>
    <div class="btn-link">@Html.ActionLink("Negative Reviews", "Negatives", "Home")</div>
</div>

@{
    ViewBag.Title = "Home Page";
}

@model List<OldNewTeamProject.Models.Language>

@for (int i = 1; i <= Model.Count; i++)
{
    <div class="row">
        <div class="language">
            <h2>@i. @Html.ActionLink(Model[i-1].Name, "Details/"+i, "Languages")</h2>
            
            <div class="ratio">
                @if (Model[i - 1].positives > 0)
                {
                    <p>Positive reviews @Model[i-1].positives</p>
                }
                else
                {
                    <p>Positive reviews 0</p>
                }
            </div>
            <ul class="posts">
                @foreach (var post in Model[i-1].Posts)
                {
                    if (post.Value == "rocks")
                    {
                        <li>@post.Content ---> @post.Value</li>
                    }
                    
                }
            </ul>

        </div>
    </div>
}